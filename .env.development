# Local development defaults (committed to repo)
# Override locally with .env.local (gitignored)

VITE_ZITADEL_AUTHORITY=http://localhost:8080
VITE_ZITADEL_CLIENT_ID=placeholder
VITE_ZITADEL_PROJECT_ID=placeholder
VITE_API_URL=http://localhost:8081
