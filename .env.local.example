# Local overrides (gitignored).
#
# Copy to .env.local and fill in values for local development.
# Production does NOT use .env files; see infra/kubernetes/.../ctx8-landing/runtime/env.js.

# ctx8-api backend URL
VITE_API_URL=http://localhost:8081

# ZITADEL OIDC
VITE_ZITADEL_AUTHORITY=http://localhost:8080
VITE_ZITADEL_CLIENT_ID=
VITE_ZITADEL_PROJECT_ID=

# Optional: Context8 Analytics (ChatKit)
VITE_CHATKIT_WORKFLOW_ID=
VITE_OPENAI_API_KEY=

# Optional: Supabase (legacy/auxiliary features)
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
