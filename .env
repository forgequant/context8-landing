# Safe defaults committed to the repo.
#
# Local development:
# - Uses `.env.development` (committed) and optional `.env.local` (gitignored).
#
# Production:
# - Should inject runtime config via `/env.js` (sets `globalThis.__CTX8_ENV`).
# - If runtime injection is missing, leaving `VITE_API_URL` unset makes the app use same-origin
#   requests (relative `/api/...`) instead of hardcoding `http://localhost:8081`.

VITE_CHATKIT_WORKFLOW_ID=
