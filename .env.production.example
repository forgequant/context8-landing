# Production environment (context8-landing)
#
# In production we deploy a static nginx container and provide runtime config via /env.js
# (not via .env files). See:
# - infra/kubernetes/apps/forgequant/prod/ctx8-landing/runtime/env.js
# - infra/terraform/zitadel/CTX8_GITOPS_HANDOFF.md
#
# This file exists only as a reference of required public variables.

VITE_API_URL=https://api.context8.markets
VITE_ZITADEL_AUTHORITY=https://auth.10g.dev
VITE_ZITADEL_CLIENT_ID=<from-terraform-output>
VITE_ZITADEL_PROJECT_ID=<from-terraform-output>
